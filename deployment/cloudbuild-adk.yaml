# Cloud Build configuration for Backtest Agent with custom Dockerfile
steps:
  # Build the Docker image using Dockerfile.adk
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/backtest-agent:v2'
      - '-t'
      - 'gcr.io/$PROJECT_ID/backtest-agent:latest'
      - '-f'
      - 'deployment/Dockerfile.adk'
      - '.'
    id: 'build-image'

  # Push the image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/backtest-agent:v2'
    id: 'push-image'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'backtest-agent'
      - '--image=gcr.io/$PROJECT_ID/backtest-agent:v2'
      - '--region=us-central1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--memory=1Gi'
      - '--cpu=1'
      - '--timeout=300'
      - '--max-instances=10'
    id: 'deploy-cloud-run'

images:
  - 'gcr.io/$PROJECT_ID/backtest-agent:v2'
  - 'gcr.io/$PROJECT_ID/backtest-agent:latest'

timeout: '1200s'
